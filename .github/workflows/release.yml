name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v0.7.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build UI
        run: |
          bun run build:review
          bun run build:hook

      - name: Compile binaries (cross-compile all targets)
        run: |
          # macOS ARM64
          bun build apps/hook/server/index.ts --compile --target=bun-darwin-arm64 --outfile specannotate-darwin-arm64
          sha256sum specannotate-darwin-arm64 > specannotate-darwin-arm64.sha256

          # macOS x64
          bun build apps/hook/server/index.ts --compile --target=bun-darwin-x64 --outfile specannotate-darwin-x64
          sha256sum specannotate-darwin-x64 > specannotate-darwin-x64.sha256

          # Linux x64
          bun build apps/hook/server/index.ts --compile --target=bun-linux-x64 --outfile specannotate-linux-x64
          sha256sum specannotate-linux-x64 > specannotate-linux-x64.sha256

          # Linux ARM64
          bun build apps/hook/server/index.ts --compile --target=bun-linux-arm64 --outfile specannotate-linux-arm64
          sha256sum specannotate-linux-arm64 > specannotate-linux-arm64.sha256

          # Windows x64
          bun build apps/hook/server/index.ts --compile --target=bun-windows-x64 --outfile specannotate-win32-x64.exe
          sha256sum specannotate-win32-x64.exe > specannotate-win32-x64.exe.sha256

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: |
            specannotate-*
            !*.ts

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set release tag
        id: tag
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "tag=${{ inputs.tag }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: binaries
          path: artifacts

      - name: List artifacts
        run: ls -la artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          files: artifacts/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(steps.tag.outputs.tag, '-') }}
